#include "../stdfuns.h"

int main() {
    const size_t alen;
    double d[len];

    for (double i = 0; i<alen; i++)
        d[i] = i;

    for (double i = 0; i<alen; i++)
        printf("d[%i] = %i", i, d[i]);

    size_t len

    return 0;
}
